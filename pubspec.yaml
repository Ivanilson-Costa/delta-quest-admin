name: delta_quest_flutter
description: Delta Quest IT Web Admin
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.12.1
  go_router: ^14.8.1
  intl: ^0.19.0
  file_picker: ^8.0.0
  excel: ^2.1.0
  cupertino_icons: ^1.0.6  
  universal_html: ^2.2.4

flutter:
  uses-material-design: true
  assets:
    - assets/templates/modelo_questionario.xlsx
    - assets/images/logo_delta_quest.png

